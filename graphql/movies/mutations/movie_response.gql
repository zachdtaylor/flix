mutation response($like: Boolean, $tmdbId: ID!) {
  response(like: $like, tmdbId: $tmdbId) {
    response {
      id
      like
      movie {
        id
        totalLikes
        totalDislikes
      }
    }
  }
}
